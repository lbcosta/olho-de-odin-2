# 1. Use Electron for Desktop Delivery

We decided to build the application as a desktop app using Electron because the target users need a simple "double-click" executable on Windows with zero setup. Electron allows us to write the entire codebase in JavaScript/TypeScript, making it easier for AI agents to develop the database, file import/export, and API requests, while accepting the trade-off of a larger application bundle size.
